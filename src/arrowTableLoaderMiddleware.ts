// import * as Redux from 'redux';
// import * as Arrow from 'apache-arrow';

// export type Load = Redux.Action<'arrow:table-load'> & { filename: string };
// export type Loaded = Redux.Action<'arrow:table-loaded'> & { table: Arrow.Table };

// type DispatchExt = {
//     (action: Load):void
// };

// export const middleware: Redux.Middleware<DispatchExt> = function (api: Redux.MiddlewareAPI) {
//     return (next: Redux.Dispatch<Redux.AnyAction>) => {
//         return (action: any) => {
//         };
//     }
// }
